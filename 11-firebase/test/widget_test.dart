// Level 11 - Firebase: placeholder test (full test butuh Firebase emulator / mock).

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
